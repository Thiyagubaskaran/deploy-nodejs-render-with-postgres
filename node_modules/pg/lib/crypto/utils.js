'use strict'

const { version: nodeVersion } = process
const useLegacyCrypto = parseInt(nodeVersion.split('.')[0]) < 15

module.exports = useLegacyCrypto ? require('./utils-legacy') : require('./utils-webcrypto')

