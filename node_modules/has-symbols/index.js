'use strict';

const hasSymbolSham = require('./shams');

module.exports = () => {
  const symbol = typeof Symbol === 'function' ? Symbol : false;
  const hasNativeSymbol = symbol && typeof symbol('') === 'symbol';

  return hasNativeSymbol && hasSymbolSham();
};
