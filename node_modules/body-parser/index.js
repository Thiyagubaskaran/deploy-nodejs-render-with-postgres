// body-parser.js

'use strict'

const json = require('./lib/types/json')
const raw = require('./lib/types/raw')
const text = require('./lib/types/text')
const urlencoded = require('./lib/types/urlencoded')

/**
 * Module exports.
 * @type {object}
 * @property {function} json
 * @property {function} raw
 * @property {function} text
 * @property {function} urlencoded
 */

module.exports = {
  json,
  raw,
  text,
  urlencoded
}



// lib/types/json.js

'use strict'

const deprecate = require('depd')('body-parser')

function json (options) {
  // ... (json parser logic)
}

module.exports = deprecate.function(json, 'bodyParser: use individual json middleware')



// lib/types/raw.js

'use strict'

const deprecate = require('depd')('body-parser')

function raw (options) {
  // ... (raw parser logic)
}

module.exports = deprecate.function(raw, 'bodyParser: use individual raw middleware')



// lib/types/text.js

'use strict'

const deprecate = require('depd')('body-parser')

function text (options) {
  // ... (text parser logic)
}

module.exports = deprecate.function(text, 'bodyParser: use individual text middleware')



// lib/types/urlencoded.js

'use strict'

const deprecate = require('depd')('body-parser')

function urlencoded (options) {
  // ... (urlencoded parser logic)
}

module.exports = deprecate.function(urlencoded, 'bodyParser: use individual urlencoded middleware')

